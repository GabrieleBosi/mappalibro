#!/usr/bin/env node
// Placeholder — the ingestion pipeline (download, strip boilerplate,
// chapters.json scaffolding) is implemented in Phase 3.
console.error('npm run ingest is not implemented yet (planned for Phase 3).');
console.error('Usage: npm run ingest -- <gutenberg-id-or-epub-path>');
process.exit(1);
